const input_file = args[1]
const part = args[2]

const f = io.open(input_file)
let line = f->read('l')
let line_count = 0
while (line) {
  line_count += 1
  line = f->read('l')
}

 echo "Received #{line_count} lines of input for part #{part}"
